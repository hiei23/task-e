<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="registration-row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(@user) do |f| %>
        <p><%= f.label :name ,:id =>"registration-label"%>
          <%= f.text_field :name %>
          <br></br>
           <%= f.label :email , :id =>"registration-label" %>
           <%= f.email_field :email %></p>

          <p><%= f.label :password ,:id =>"registration-label"%>
           <%= f.password_field :password %>
            <br></br>
          <%= f.label :password_confirmation, "Confirmation" ,:id =>"registration-label"%>
           <%= f.password_field :password_confirmation %></p>

          <p> <%= f.submit "Submit" , :id=> "button"%></p>
      <% end %>
    </div>
  </div>

<% end %>
